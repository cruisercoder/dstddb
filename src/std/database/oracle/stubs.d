module std.database.oracle.stubs;

public import std.database.oracle.bindings;

import std.stdio;

extern(System) {

    /*
    sword OCIEnvCreate (
            OCIEnv **envhpp,
            ub4 mode,
            const dvoid *ctxp,
            //const dvoid *(*malocfp) (dvoid *ctxp, size_t size),
            //const dvoid *(*ralocfp) (dvoid *ctxp, dvoid *memptr, size_t newsize),
            //const void (*mfreefp) (dvoid *ctxp, dvoid *memptr)) size_t xtramemsz,
          const dvoid *,
          const dvoid *,
          const dvoid *,
          dvoid **usrmempp ) {
              writeln("(OCIEnvCreate)");
              //*envhpp = cast(void *) 123;
              return 0;
          }

    sword OCIHandleFree(void *hndlp, const ub4 type) {
        writeln("(OCIHandleFree)");
        return 0;
    }
    */
}

